# Lingua Link Backend Configuration Template
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database Configuration (Required)
# Get this from your Neon database dashboard
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# JWT Configuration (Required)
# Generate a secure random string for production
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# Email Configuration (Optional - for user notifications)
# Get these from your Resend account: https://resend.com
RESEND_API_KEY=re_your_resend_api_key_here
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=LinguaLink

# DeepL Translation API Configuration (Required)
# Get this from your DeepL account: https://www.deepl.com/pro-api
# Free tier keys end with :fx, Pro tier keys don't have suffix
DEEPL_API_KEY=your_deepl_api_key_here

# Rate Limiting Configuration
ALLOW_DEV_RATE_LIMITS=true

# Production Environment Variables (for deployment)
# NODE_ENV=production
# CLIENT_URL=https://your-frontend-domain.vercel.app

# Security & Production Notes:
# 1. Never commit .env files to version control
# 2. Use strong, unique passwords and API keys (32+ characters for JWT_SECRET)
# 3. Rotate credentials regularly
# 4. Ensure DATABASE_URL uses SSL (sslmode=require)
# 5. Each backend instance should have its own DeepL API key for maximum capacity
# 6. Use environment-specific configurations for dev/staging/production